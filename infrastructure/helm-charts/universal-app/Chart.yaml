apiVersion: v2
name: universal-app
description: A universal Helm chart for deploying multi-component applications from JSON manifests
type: application
version: 1.0.0
appVersion: "1.0.0"

keywords:
  - universal
  - microservices
  - idp
  - self-service

maintainers:
  - name: Platform Team
    email: platform@example.com

# Note: For MVP, we use a built-in simple PostgreSQL StatefulSet (databases.simplePostgresql)
# To use Bitnami PostgreSQL instead, uncomment below and run: helm dependency update
# dependencies:
#   - name: postgresql
#     version: "15.x.x"
#     repository: "https://charts.bitnami.com/bitnami"
#     condition: databases.postgresql.enabled

